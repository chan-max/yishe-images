# 🔧 修复镜像名称问题

## ❌ 问题

如果你的 Docker Hub 用户名不是 `1s`，但镜像名称是 `1s/yishe-images:latest`，推送会失败。

**错误提示：**
```
denied: requested access to the resource is denied
```

---

## ✅ 解决方案：重新打标签

### 步骤 1：确认你的 Docker Hub 用户名

登录 Docker Hub 网站查看，或者运行：
```powershell
docker login
```
登录时输入的用户名就是你的 Docker Hub 用户名。

### 步骤 2：重新打标签

使用 `docker tag` 命令给镜像打新标签：

```powershell
# 格式：docker tag 旧名称:标签 新名称:标签
docker tag 1s/yishe-images:latest 你的用户名/yishe-images:latest

# 示例（假设你的用户名是 zhangsan）
docker tag 1s/yishe-images:latest zhangsan/yishe-images:latest
```

**说明：**
- `1s/yishe-images:latest`：现有的镜像名称
- `你的用户名/yishe-images:latest`：新的镜像名称（使用你的 Docker Hub 用户名）

### 步骤 3：验证新标签

```powershell
docker images
```

应该能看到两个镜像（实际上是同一个镜像，只是标签不同）：
```
REPOSITORY                 TAG       IMAGE ID
1s/yishe-images           latest    abc123def456
你的用户名/yishe-images   latest    abc123def456  (同一个镜像)
```

### 步骤 4：推送正确的镜像

```powershell
docker push 你的用户名/yishe-images:latest
```

---

## 📋 完整操作流程

假设你的 Docker Hub 用户名是 `zhangsan`：

```powershell
# 1. 登录 Docker Hub
docker login
# 输入用户名：zhangsan
# 输入密码：你的密码

# 2. 重新打标签（使用你的真实用户名）
docker tag 1s/yishe-images:latest zhangsan/yishe-images:latest

# 3. 验证
docker images | findstr "yishe-images"

# 4. 推送正确的镜像
docker push zhangsan/yishe-images:latest

# 5. 完成！
```

---

## 🗑️ 清理旧标签（可选）

推送成功后，可以删除旧的标签：

```powershell
# 删除旧标签（镜像本身不会被删除，只是删除标签）
docker rmi 1s/yishe-images:latest
```

---

## 💡 重要提示

1. **镜像名称格式**：必须是 `你的DockerHub用户名/镜像名:标签`
2. **用户名必须匹配**：镜像名称中的用户名必须与登录的用户名一致
3. **重新打标签不会复制**：`docker tag` 只是创建新标签，不会复制镜像（节省空间）
4. **可以保留多个标签**：同一个镜像可以有多个标签

---

## 🔍 如何查看你的 Docker Hub 用户名？

### 方法 1：访问网站
- 打开 https://hub.docker.com/
- 登录后，右上角显示的就是你的用户名

### 方法 2：查看登录信息
```powershell
docker info | findstr "Username"
```

### 方法 3：尝试登录
```powershell
docker login
```
输入的用户名就是你的 Docker Hub 用户名

---

## ✅ 检查清单

推送前确保：

- [ ] 知道你的 Docker Hub 用户名
- [ ] 镜像名称格式正确（`用户名/镜像名:标签`）
- [ ] 已登录 Docker Hub
- [ ] 镜像已重新打标签（如果用户名不匹配）

---

**现在按照上面的步骤操作即可！** 🚀

